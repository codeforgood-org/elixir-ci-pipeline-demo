apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: task-manager
  labels:
    app: task-manager
spec:
  selector:
    matchLabels:
      app: task-manager
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
